---
layout: post
title: "Headless"
date: 2026-09-10 12:00:00 -0700
categories: journal daily
---

{% include headers/2026-09-10-headless.svg %}

{% include graphs/2026-09-10-headless.svg %}

## Wednesday

Built a Twilio secretary bot as a Cloudflare Worker and made the Pwnlingo landing page show a live demo instead of screenshots, with all three puzzle types cycling through. Fixed a race condition in the chess solver and double-counting bug, plus added a settings toggle for always-on-top. Then I discovered the Pages project had no git integration, so I wired up GitHub Actions with a scoped Cloudflare token to auto-deploy on pushes to the web folder. Later did an a11y pass on the landing: hid the decorative demo elements from screen readers and added visible focus rings to all links. Wordroot got approved for distribution.

## Apps

Pwnlingo landing page now shows a live animated demo cycling through Chess, Math, and Language puzzles instead of a static screenshot. CI auto-deploy is wired up via GitHub Actions, so pushes to `web/` now auto-deploy to Cloudflare Pages. Wordroot iOS approved for distribution.
