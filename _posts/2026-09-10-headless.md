---
layout: post
title: "Headless"
date: 2026-09-10 12:00:00 -0700
categories: journal daily
---

{% include headers/2026-09-10-headless.svg %}

{% include graphs/2026-09-10-headless.svg %}

## Wednesday

Spent the morning debugging Voxprint's background downloads and Pwnlingo's UI, then fixed Epiphany's portfolio balance: the account adapter was silently zeroing unpriced positions, understating the real total. Wired up multi-brokerage connect across web, iOS, macOS, and desktop (Kotlin). Later, exposed live math and language prompts on Pwnlingo's landing demo. Now when the runner switches subjects, the page shows the real problem text instead of a frozen chessboard, plus live streak and solve-rate stats. Deployed via staging to work around Cloudflare's file-size limit.

## Thursday

Built animated chess board with move-by-move pieces, pause/resume, progress tracking. Fixed persistence, cache, FEN bugs; swapped emoji for unicode; iOS launch bug (Info.plist); KMP brought Android/Desktop live with native-release.yml CI. Session-first auth made jwt.txt optional, security hardened with token-gating and rate limits. Added four new surfaces: watchOS glanceable, macOS MenuBarExtra XP, iOS widget, SwiftTUI TUI. Found real bug: ISO8601DateFormatter rejected fractional timestamps, killing elapsed/XP/min/STUCK (fixed with shared formatter); landing shows live runner via /api/live edge-cached Function; seven platforms shipped: web, macOS (app/menu bar), iOS (app/widget), watchOS, Android, Windows, Linux, terminal, and finished the app's control side with a Start button that launches the solver as a background process, a toggle for running it when closed, a /quit route for stopping across relaunches, and an alert when the solver exits due to a missing Duolingo session.

## Apps

Pwnlingo: animated board, pause/resume, stats, sound, languages. Wordroot: Wikipedia photos, search label. Homeqi: multi-property. Voxprint: background downloads, live animation. Epiphany: portfolio balance fix, multi-brokerage connect (web/iOS/macOS/Android/desktop).
