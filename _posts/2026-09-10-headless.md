---
layout: post
title: "Headless"
date: 2026-09-10 12:00:00 -0700
categories: journal daily
---

{% include headers/2026-09-10-headless.svg %}

{% include graphs/2026-09-10-headless.svg %}

## Wednesday

Built a Twilio secretary bot as a Cloudflare Worker and made the Pwnlingo landing page show a live demo instead of screenshots, with all three puzzle types cycling through. Fixed a race condition in the chess solver and double-counting bug, plus added a settings toggle for always-on-top. Then I discovered the Pages project had no git integration, so I wired up GitHub Actions with a scoped Cloudflare token to auto-deploy on pushes to the web folder. Later did an a11y pass on the landing: hid the decorative demo elements from screen readers and added visible focus rings to all links, then added clickable track buttons so users can pin a puzzle type instead of just auto-cycling. Wordroot got approved for distribution, and ran a fleet-wide lint pass to clean up junk files (11 stray `.DS_Store`, one `.bak`, and several zero-byte tracked files), updating the lint skill to catch these automatically.

## Chess debugged

Later in the day, Pwnlingo's chess solver got stuck in an infinite loop. Found the bug: the pawn-promotion picker always clicked the queen's slot, so underpromotion puzzles (rook, bishop, knight finales) graded wrong and got reshuffled endlessly. Wrote a self-calibrating piece picker that learns the slot index for each piece from correct grades, so the bot now solves every underpromotion puzzle correctly. Moved the solver code from dotfiles to its own GitHub repo checkout at pwnlingo, synced both, committed it, and pushed. The live process still runs out of dotfiles where its browser profile lives, but the two repos are now in sync and tracked properly.

## Thursday

Built an animated chess board with pieces sliding move-by-move, added pause/resume, fixed window-size persistence, screenshot cache-busting, and font-substitution bugs. Hardened parseBoard() for malformed FEN, fixed pause bug (per-move check), set up Cloudflare Tunnel. Design refinement: switched to Helvetica for real unicode chess glyphs (proper knight/crowns, no emoji). Added per-puzzle progress indicator (puzzleStep/puzzleTotal). Refined layout: contained board top-aligned, info panel below with glass depth.

## Apps

Pwnlingo: animated board, pause/resume, stats, sound, menu languages, theme, tunnel. Wordroot iOS approved.
