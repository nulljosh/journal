---
layout: post
title: "Headless"
date: 2026-09-10 12:00:00 -0700
categories: journal daily
---

{% include headers/2026-09-10-headless.svg %}

{% include graphs/2026-09-10-headless.svg %}

## Wednesday

Built a Twilio secretary bot as a Cloudflare Worker and reworked Pwnlingo's landing to show the live demo instead of screenshots. Fixed a race condition in the chess solver and double-counting bug, wired up GitHub Actions for auto-deploy on web folder pushes. Did an a11y pass: screen-reader hiding for decorative elements, focus rings on links, clickable track buttons for puzzle pinning. Wordroot approved. Ran fleet-wide lint pass to clean up junk files (11 `.DS_Store`, one `.bak`, zero-byte tracked files).

## Chess debugged

Pwnlingo's solver got stuck in an infinite loop: pawn-promotion picker always clicked the queen's slot, failing underpromotion puzzles. Built a self-calibrating picker that learns each piece's slot from correct grades. Moved solver code from dotfiles to its own GitHub repo checkout, synced both, committed.

## Thursday

Built an animated chess board with move-by-move pieces, pause/resume, fixed persistence and cache-busting bugs. Hardened parseBoard() for malformed FEN. Design refinement: Helvetica unicode glyphs instead of emoji. Added per-puzzle progress and refined layout with glass-depth panels.

## Documentation pass

Added clarifying comments to nimble's QueryEngine and epiphany's PeoplePanel (first batch). Expanded with comment-only improvements to authmail, bcgd, breathe, cadence, conway, dream, healstack, homeqi, lexly, plain, plan, voxprint, and wordroot (second batch), explaining complex heuristics, regex patterns, state logic, and API interaction code. All comments are in-place documentation; no functional changes across both passes.

## Landing polish

Expanded Pwnlingo's hero background floating icons from six to ten and repositioned the Read the Source CTA button below the live demo instead of above it, improving visual hierarchy. Deployed to pwnlingo.heyitsmejosh.com via Cloudflare Pages.

## Landing animations rollout

Shipped scroll-reveal and parallax animations to 28 apps across the fleet (nyc, wordroot, charwork, plain, numen, curvely, bcgd, talli, bookrank, breathe, cadence, conway, curbfind, dream, homeqi, inkpress, lexly, litigate, plan, quotestreak, seamark, sidewise, tripwire, voxprint, weather, roost, healstack, homeward). Each landing page now reveals sections on scroll with subtle motion and parallax depth. Skipped epiphany due to unrelated diff changes. All commits pushed.

## Apps

Pwnlingo: animated board, pause/resume, stats, sound, menu languages, theme, tunnel. Wordroot iOS approved.
