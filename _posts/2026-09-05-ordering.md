---
layout: post
title: "Ordering"
date: 2026-09-05 12:00:00 -0700
categories: journal daily
---

{% include headers/2026-09-05-ordering.svg %}

Started a personal security learning project called hackrange. Built a pentest track with OWASP Juice Shop to practice web exploits, an RE track with C crackmes for binary analysis, and a leetcode track stub. Walked through Juice Shop challenges live in Chrome to see the attacks work rather than just read about them. Pushed to private GitHub repo.

Fixed roost's landing page routing so it stays at the landing page instead of auto-redirecting logged-in users to /browse. Deployed to roost.heyitsmejosh.com.

Fixed homeward's landing page header, which was trapped inside a centered container and sat 200px from the page edge on wide screens. Moved it out and pinned it to the viewport corner with absolute positioning. Deployed to homeward.heyitsmejosh.com.

Renamed curbside to curbfind (App Store name was taken). Gave it a rust-orange accent (#cc5200) scoped locally so tokens.css stays untouched. Replaced the icon from a price-tag box to a location pin. Deployed to curbfind.heyitsmejosh.com.

Built terminal user interfaces for 16 apps using SwiftTUI: nimble, cadence, numen, wordroot, keyrate, curvely, charwork, bookrank, quotestreak, inkpress, tripwire, sidewise, nyc, curbfind, homeward, roost. Each got a tui/ folder and Package.swift entry point. The terminals are one-shot static renders fetching from live APIs or reusing existing logic, all requiring a real TTY.

Switched curvely's color palette from Jaybulb tokens to clrs.cc. Added interactive device-frame demos to charwork and numen's landing pages, each embedding the client-side engine live in an iPhone 15 frame.

Fixed the portfolio ranking rule on heyitsmejosh.com. Litigate doesn't have an App Store link so it shouldn't sit in the App Store tier; moved it to the "more" list, bumping that count from 15 to 16.

Ran a three-tier security sweep across all 37 codebases under ~/Documents/Code, checking for hardcoded secrets, eval/exec/pickle calls, XSS sinks, permissive CORS, weak crypto, raw SQL concat, chmod 777, logged secrets, tracked .env files, auth bypass TODOs, and exposed SUPABASE_SERVICE_ROLE. Everything came back clean.

Apps: Plain ships iOS and macOS. breathe live (web). bookrank with screenshot-to-summary. Talli with legal aid directory. Fleet apps on Kotlin ports. Homeward header fixed, curbfind renamed and refreshed.
