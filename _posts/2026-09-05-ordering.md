---
layout: post
title: "Ordering"
date: 2026-09-05 12:00:00 -0700
categories: journal daily
---

{% include headers/2026-09-05-ordering.svg %}

Started hackrange, a security learning dojo with three tracks: pentest (OWASP Juice Shop), reverse engineering (C crackmes), and algorithms (LeetCode stub). Walked through Juice Shop challenges live in Chrome to see attacks rather than read about them. Pushed to private GitHub.

Fixed roost's landing page routing so it stays at the landing page instead of auto-redirecting logged-in users to /browse. Deployed to roost.heyitsmejosh.com.

Fixed homeward's landing page header: it was trapped inside a centered container, 200px from the edge. Pinned it to the viewport corner. Deployed.

Renamed curbside to curbfind (App Store name was taken). Gave it a rust-orange accent (#cc5200) scoped locally so tokens.css stays untouched. Replaced the icon from a price-tag box to a location pin. Deployed to curbfind.heyitsmejosh.com.

Built 16 terminal UIs with SwiftTUI (nimble, cadence, numen, wordroot, keyrate, curvely, charwork, bookrank, quotestreak, inkpress, tripwire, sidewise, nyc, curbfind, homeward, roost). Each got tui/ folder and Package.swift. One-shot static renders fetching live APIs or reusing logic, all needing real TTY.

Switched curvely's color palette from Jaybulb tokens to clrs.cc. Added interactive device-frame demos to charwork and numen's landing pages, each embedding the client-side engine live in an iPhone 15 frame.

Fixed the portfolio ranking rule on heyitsmejosh.com. Litigate doesn't have an App Store link so it shouldn't sit in the App Store tier; moved it to the "more" list, bumping that count from 15 to 16.

Ran a security sweep across all 37 codebases checking for hardcoded secrets, eval, XSS, CORS, weak crypto, raw SQL, permissions, leaked secrets, .env files, and auth TODOs. Everything clean.

Submitted curbfind to the App Store (iOS and macOS, free, 175 territories). Reworked the landing hero with blurred iframe and scroll-snap so clicking un-blurs and zooms seamlessly with interaction. Fixed deploy bug (wrong directory), removed redundant screenshot, tuned colors.

Apps: Plain ships iOS and macOS. breathe live (web). bookrank with screenshot-to-summary. Talli with legal aid directory. Fleet apps on Kotlin ports. Homeward header fixed, curbfind renamed and refreshed.
