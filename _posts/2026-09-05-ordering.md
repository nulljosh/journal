---
layout: post
title: "Ordering"
date: 2026-09-05 12:00:00 -0700
categories: journal daily
---

{% include headers/2026-09-05-ordering.svg %}

Started hackrange, a security learning dojo with pentest, reverse engineering, and algorithm tracks. Walked through Juice Shop challenges live in Chrome, pushed to private GitHub. Fixed roost and homeward's landing pages, both had layout bugs. Renamed curbside to curbfind (App Store name was taken), gave it a rust-orange accent and location-pin icon.

Built 16 terminal UIs with SwiftTUI across the fleet (nimble, cadence, numen, wordroot, keyrate, curvely, charwork, bookrank, quotestreak, inkpress, tripwire, sidewise, nyc, curbfind, homeward, roost). Each has tui/ folder and static Package.swift.

Switched curvely's palette to clrs.cc. Added interactive device-frame demos to charwork and numen's landing pages. Fixed portfolio ranking so Litigate sits in "more" instead of App Store tier. Ran a security sweep across all 37 codebases (everything clean). Submitted curbfind to the App Store with a reworked hero that's the live app itself, blurred and clickable to zoom in.

Got curbfind's AI deal-ranking working. Built a sort that scores listings against the search's median price and tags the top ones with why they're good via Cloudflare's AI. Wired it into all four platforms: web dropdown, iOS/macOS route through the worker, Android/desktop toggle. The AI model had been deprecated so it was failing silently since launch; switched to the current model, added error logging, and verified live. Created a skill to surface deals in chat. Made the curbfind repo public. Hit a second bug while testing: the AI model ignores the requested JSON shape and replies with an array instead of flat objects, silently zeroing the reasons. Fixed the parser to accept both shapes, added two more tests (42 total now), redeployed, and verified live. Brought the repo up to house standards: added CLAUDE.md and architecture.svg, fixed the stale GitHub homepage URL, and refreshed the README screenshot to show deals with real reasons visible.

Apps: Plain ships iOS and macOS. breathe live (web). bookrank with screenshot-to-summary. Talli with legal aid directory. Fleet on Kotlin ports. Curbfind deals sorted and public.
