---
layout: post
title: "Ordering"
date: 2026-09-05 12:00:00 -0700
categories: journal daily
---

{% include headers/2026-09-05-ordering.svg %}

Started hackrange, a security learning dojo with pentest, reverse engineering, and algorithm tracks. Walked through Juice Shop challenges live in Chrome, pushed to private GitHub. Fixed roost and homeward's landing pages, both had layout bugs. Renamed curbside to curbfind (App Store name was taken), gave it a rust-orange accent and location-pin icon.

Built 16 terminal UIs with SwiftTUI across the fleet (nimble, cadence, numen, wordroot, keyrate, curvely, charwork, bookrank, quotestreak, inkpress, tripwire, sidewise, nyc, curbfind, homeward, roost).

Switched curvely's palette to clrs.cc. Added interactive device-frame demos to charwork and numen's landing pages. Fixed portfolio ranking so Litigate sits in "more" instead of App Store tier. Ran a security sweep across all 37 codebases (everything clean). Submitted curbfind to the App Store with a reworked hero that's the live app itself, blurred and clickable to zoom in. Fixed the landing zoom afterwards, since the tint lingered instead of fading with the page.

Got curbfind's AI deal-ranking working. Built a sort that scores listings against the search's median price and tags the top ones with why they're good via Cloudflare's AI. Wired it into all four platforms: web dropdown, iOS/macOS through the worker, Android/desktop toggle. The AI model had been deprecated and failing silently since launch; switched to the current one, added error logging, verified live. Hit a second bug testing it: the model ignored the requested JSON shape and silently zeroed the reasons. Fixed the parser to accept both shapes, added tests, redeployed. Brought the repo up to house standards and made it public.

Apps: Plain iOS/macOS. breathe web. bookrank. Talli. Fleet ports. Curbfind deals live.

Built three new Claude Code skills: project-sync, screenshot-refresh, domain-audit. project-sync filed a missing architecture diagram as a roadmap item in 15 repos. domain-audit checked all 33 subdomains; all answered except brain's intended login wall. screenshot-refresh opened each site in Chrome and dropped a current screenshot into 17 repos. Caught none of it was committed, then pushed to 18 repos. breathe has no remote, so its commit stays local.
