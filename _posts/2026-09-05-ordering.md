---
layout: post
title: "Ordering"
date: 2026-09-05 12:00:00 -0700
categories: journal daily
---

{% include headers/2026-09-05-ordering.svg %}

Started a personal security learning project called hackrange today. Built a pentest track with OWASP Juice Shop to practice web exploits (XSS, SQL injection), an RE track with C crackmes for binary analysis via lldb and otool, and a leetcode track stub. Walked through a couple Juice Shop challenges live in Chrome to actually see the exploits work rather than just reading about them. Pushed to a private GitHub repo since this is personal learning, not shipping. This is what I've meant to do with Kali for years but never actually sat down to try.

Also fixed roost's landing page routing: the app was auto-redirecting logged-in users straight to /browse, which defeated the whole marketing purpose of the landing page. Now "/" stays at the landing page for everyone. Deployed to roost.heyitsmejosh.com.

Fixed homeward's landing page header: the topbar was caught inside the centered column container, so on wide screens it sat about 200 pixels from the page edge instead of being pinned top-left like a normal site header. Moved it out of the container and pinned it to the actual viewport corner with absolute positioning. Deployed to homeward.heyitsmejosh.com.

Gave curbside its own visual identity instead of using the shared Jaybulb yellow. Swapped in a rust-orange accent (#cc5200) locally scoped just to the web build, so tokens.css stays untouched and the other apps aren't affected. Recolored the header mark, search button, save star, and outbound links to match. Deployed to curbside.heyitsmejosh.com.

Apps: Plain ships iOS and macOS. breathe live (web MVP). bookrank with screenshot-to-summary. Talli with local notifications and legal aid directory. All fleet apps on real Kotlin ports. Homeward header fixed, curbside color scheme.
