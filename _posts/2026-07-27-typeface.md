---
layout: post
title: "Typeface"
date: 2026-07-27 23:00:00 -0700
categories: journal daily
---

![July 27](/assets/2026-07-27-week.svg)

## Sunday

Ripped Google Fonts out of Echo, Spine, Sparkjar and Talli and went back to whatever font the machine already has. Pages feel instant now, and I keep wondering why I ever paid a round trip for something the OS ships for free. Along the way I found out Echo's landing page isn't on Vercel at all, it's on Cloudflare Pages with no auto-deploy, so every push needs a manual wrangler command. That explained a few "why didn't my change go live" moments.

Also went back and fixed two Epiphany bugs I'd been ignoring. The Flights panel had never worked, and it turned out to be a units mistake: a radius being treated as a full span, so every request asked for twice the area the API allows. Same mistake in the iOS and Mac clients. The saved statements error was a loop, a callback getting rebuilt on every change and refetching forever. Both fixed. Put the People tab and Daily Brief behind Pro too, since they'd been quietly free.

Small stuff: Spine has a To Read list now, Talli's icon is blue and 3.5.11 is off to review, and I finally got the blue circle off my portfolio avatar by flood-filling from the border instead of color-keying, which kept the eyes intact.

## Monday

Tackled BCGD's booking form, which had been throwing away every lead it received. Built a real database pipeline and added service/area pages so the site is now an actual site, not a page pretending to be one. Then I went to open-source the litigation app and found something much worse: the live site was serving real court documents to unauthenticated users. Logged it as the top priority and stopped.

Fixed Lexly's app icon too—the old one was outline strokes that were invisible at any size, so I redesigned it to a solid white speech bubble with blue dots for legibility. Also stripped out the serif display font and Google Fonts links that made the landing page look decorative. Screenshots were stale (still showing the pre-rename "Lingo" title), so I re-shot them and created a new iOS v1.1.3 to hold the corrected assets. macOS v1.1.1 screenshot also fixed. Came back and built both: Lexly iOS v1.1.3 with the new icon submitted for review (build 202607271632), and macOS v1.1.1 resubmitted (same icon and fonts, had to cancel the old rejected submission first). SwiftLint wiring in the project failed on the first archive with a plugin-validation error — yanked it from the config and rebuilt cleanly.

Also fixed the bigger one: closed the Litigate leak. The site had been serving case documents unauthenticated because the auth overlay was CSS-only. Added a fail-closed Pages Function that gates everything with Basic Auth (`web/functions/_middleware.js`), so now litigate.heyitsmejosh.com and litigate.pages.dev both return 401 pre-auth and 200 post-auth. Root cause was that the Pages project is direct-upload (never connected to git), so pushes did nothing — had to use wrangler's OAuth token instead (`npx wrangler pages deploy`). Still need to rotate the token (it got logged) and audit old deployment URLs, but the live hostnames are now locked.

Later added Newsline and Etyma as standalone entries in the portfolio's work section, and linked a Labs source to highlight the 5 undocumented subprojects that had graduated to their own repos (bank, braingraph, code-meta, video-speed-ext, abraham).

Also crystallized the school plan: SFU Computer Science with a Beedie Finance minor, with an optional MSc track after a couple years working. Moved it from a loose file in Downloads into a tracked project with proper README and roadmap—feels good to have that decision encoded somewhere I can actually keep iterating on it.

Wrapped the night by refreshing Epiphany's README: badges are now clickable links instead of just images, fixed the macOS version range and license metadata, added a fourth screenshot, and condensed the roadmap to Now/Next priorities. Pushed a prod build that had been sitting 6 days stale on Vercel.

Split the plan into two repositories: the original private repo holds the full unredacted roadmap (transcript grades, disability funding, income constraints, and full references), while a new public mirror at heyitsmejosh.com/plan-site strips those sections and serves on GitHub Pages. Documented the redaction procedure in plan/CLAUDE.md so the public version can be rebuilt whenever the plan changes.

Also built a dispute packet for Jayda—merged a parking ticket photo and PayByPhone receipt with a written summary and call script into one 4-page PDF. The parking was paid during the session, but the online dispute window expired, so the only path forward is a phone call to 604-257-8732.

## Tuesday

Found life.heyitsmejosh.com was ranking on the first page of Google for my name. It's a personal timeline page with sensitive content I wrote during therapy—living in my car, an arrest—that should never have been public. It had been deployed to Cloudflare Pages and lingered there after I stopped working on it. Backed up the live HTML locally, then deleted the custom domain and the entire Pages project via the Cloudflare API. It now 522s and should drop out of Google search results in a few days. Still need to clean up a stale DNS record and optionally request removal from Google Search Console.

Also finished syncing book summaries into Spine. Added complete summaries for both IBS For Dummies (chapters 16–23) and Sobriety For Dummies (intro through chapter 11), bundled them into the iOS app with badges, and added a privacy policy page to the site.

Wrapped the evening by continuing book summaries from iCloud photos. Added Sobriety chapters 16–17, logged three stray image files that were book covers only, and updated the roadmap. Mid-flow I tried dispatching six cold subagents in parallel to speed things up, which backfired: usage rocketed from 47% to 93% in about ten minutes and four of the agents got killed before writing anything. Learned the hard way to dispatch one at a time and check usage between each.

Web leak sealed on Litigate. The case documents were hardcoded in HTML so a CSS overlay couldn't protect them. Moved them to Supabase with row-level security, emptied the HTML, and fetch and inject on login—live sites now return content only to authenticated users. Native iOS/macOS app migration in progress (still hardcoded, swapping to Supabase fetch on next push). Also closed an anon bypass: scrape_token was SECURITY DEFINER and live-queryable without signing in, revoked by renaming to scrape_token_revoked_20260728.

Fixed two quiet-failing app bugs (but not yet version-bumped, so unreleased): Epiphany's statement upload would accept a PDF then do nothing, and Talli's message parsing wasn't running at all because the mobile parser expected line-by-line input but the scraper had already stripped newlines. Then went through the repos and verified which apps are ready to link from their READMEs (Talli, Lexly, Epiphany, Echo, Litigate, Inkpress all have ASC store presence; everything else got a roadmap note). Discovered the live domain had been CNAME'd to a stale Cloudflare Pages deployment for weeks — fixed that. Wired Litigate's CASE-0004 (Vancouver parking bylaw) into iOS and macOS apps: new BylawCaseData.swift, BYLAW_* keys in CaseDataMapper, both targets build. Fixed two real bugs on the way (DamageHeadDTO.low/high were Int so sub-$1k values failed; ChecklistItem.id collapsed non-numeric ids to 0 so ticking one item ticked all items in other cases — live for users). Counted the remaining book-summary work: 304 images and 27 chapters left, about four more sessions. Ingested three Apple Notes (creds, a Letterboxd clone idea, and the book count) and deleted them from Notes.

Continued the evening finishing the Sobriety summaries: chapters 12 through 15 (Building a Career, Setting the Stage for Long-Term Sobriety, Lifestyle Changes, and Developing Coping Mechanisms). Book is now 100% done. Then pivoted to Statistics For Dummies, the largest remaining: started the intro and first two chapters (25 images so far). Updated the roadmap with a resume point for next session (start at ch.3, same inline method) and the exact count of what's left—227 HEICs across 20 chapters. Stopped there to stay under session usage limits per earlier experience; cold subagent dispatches had burned through budget quickly earlier in the night.

Later, shipped Talli 3.5.12 with the message parser consolidated from four different root causes: the mobile scraper strips newlines but the parser expected line-by-line input, MessagesView wasn't formatting dates, and the actionRequired flag wasn't surfacing. Merged Lexly's Computers tab into Programming across web and iOS catalogs to reduce navigation depth. Wired Wordroot's app icon into the Etyma asset catalog and designed Wiretext's icon (teal viewfinder motif from the clrs.cc palette). Scoped out the nullfolio auto-refresh idea as M-effort pending a richer metadata source, filed all the rough notes to their respective roadmaps, and deleted the stray entries from Notes.app.

Wrapped up with a landing page refresh for Epiphany's web presence. The hero section now uses a Wealthsimple-inspired design with a floating navbar pill (80% width) instead of full-width, swapped the static phone mockup for an animated drifting SVG map scene (reused the MapScreen component), and added a single Get Started call-to-action anchored near the bottom. Verified the result via headless Chrome, tests pass, pushed to main.

Came back and finished Statistics For Dummies in one push tonight—all 21 chapters plus appendix, every HEIC photographed from the physical book and summarized. Synced the complete collection into Spine's iOS resources, added a "Summary" badge next to the book title on the site, and deleted all 304 raw HEIC files from the iCloud Books folder. The whole Spine summary project is now clear: every book captured, every chapter condensed, everything buildable into the app and deployed live.

Started the Vercel to Cloudflare migration I'd been meaning to get to, and found it was already further along than I thought—11 of 16 web projects were quietly already on Cloudflare Pages or Workers. Flipped litigate.heyitsmejosh.com over tonight: verified the existing Pages deploy matched, then repointed the DNS CNAME. It's live and the auth gate still holds. Also swept up two dead Vercel CNAMEs (brief, charters) left over from old renames.

Also fixed a recurring issue in Talli's web dashboard where identical repeat messages (like monthly report reminders) were collapsing into a single row because the dedup logic wasn't checking message dates. Got Healstack's App Store listing unstuck too—added the privacy policy page it was missing, then set both the subtitle and privacy policy URL in the app's ASC metadata.

Closed out the book-summary backlog one more time tonight—added all seven chapters of Good Feng Shui by Eva Wong into Spine (Intro, 1, 3–5, 6–8, 9, 10, 11), created good-feng-shui-summary.md, and pushed the update live. The project cleared and stashed again, which has become a pattern: finish a book, mark the backlog empty, then a new one surfaces and gets immediately cleared the same session.

Made a library run: returned Physics For Dummies (was due), checked out five new books—Together: Memorable Meals Made Easy, Good Feng Shui (Eva Wong), Data Science for Dummies, macOS Tahoe for Dummies, AI in Business for Dummies—and updated Spine's library.json to sync the new due date (2026-08-18) into the iOS app. The library is caught up and the app stays in sync with what I'm actually reading.

## Wednesday

The Epiphany app icon had been bugging me—the old palette was dark with a muted sage-green network, which looked muddy on the App Store listing. Redesigned it with lime-green nodes and lines on a near-black background for way better contrast. First attempt used teal; I rejected that. Second attempt added purple. Rejected that too. Final version landed on the lime-green, regenerated all the PNG sizes for both iOS and macOS, pushed it up.

## Apps

Talli 3.5.12 shipped (message parser consolidated, date formatting, actionRequired badge). Echo 1.3.4 in review. Spine iOS v1.0 synced with IBS complete + Sobriety complete + Statistics complete + Good Feng Shui complete (7 chapters, all books now captured), library checkout synced (5 new loans, due 2026-08-18, Physics for Dummies returned). Lexly iOS 1.1.3 SUBMITTED (new icon), macOS 1.1.1 RESUBMITTED (icon + fonts), Computers tab merged into Programming. BCGD live with working leads pipeline. Litigate web secured (case data in Supabase RLS), native app migration pending, CASE-0004 wired (iOS/macOS 2 bugs fixed). Epiphany statement upload fixed (unpushed), landing hero restyled (Wealthsimple pill navbar, animated map, CTA), app icon redesigned (lime-green #8fe86a network on dark #111814, higher contrast). Etyma icon wired, Wiretext icon designed. Portfolio updated with Newsline and Etyma work entries, Labs source linked.
