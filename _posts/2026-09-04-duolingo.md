---
layout: post
title: "Duolingo"
date: 2026-09-04 12:00:00 -0700
categories: journal daily
---

{% include headers/2026-09-04-duolingo.svg %}

Refreshed the Duolingo bot with a new icon. Looked at the solver script (21K lines, subtle interdependencies) but decided a trim was too risky, so filed it as roadmap work instead.

Dug into Epiphany's broken macOS build. Old gitignored artifacts were causing "Multiple commands produce" errors; deleted them and the tab bar rendered again. The same garbage was breaking the screenshot pipeline (README screenshot had drifted stale). Fixed a production map bug too: Overpass API mirrors were rejecting requests without a User-Agent header, so swapped in working mirrors and added the header.

Local model setup for opencode: fixed an oMLX streaming hang, discovered Qwen models emit broken tool-call format, switched to llama3.1 (correct format), uninstalled a 7GB unused model. Built a skill (renamed cheap-fix) to delegate repo chores to local models and used it across most of the codebase: added missing package descriptions, synced keywords and GitHub repo descriptions, fixed version drift, patched gitignore gaps. It caught two hallucinated outputs and a couple of false-positive gitignore checks along the way; corrected those by hand before committing. Also let it fix a real bug: sidewise's outlet count had drifted to 17 across four files while the actual feed count is 16, llama rewrote each stale line correctly.

Apps: Plain ships iOS and macOS (web landing live). breathe live (web MVP). bookrank with screenshot-to-summary. All fleet apps on real Kotlin ports now.
