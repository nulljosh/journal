---
layout: post
title: "Fixes"
date: 2026-09-02 12:00:00 -0700
categories: journal weekly
---

{% include headers/2026-09-02-fixes.svg %}

Closing holes across six apps. Talli's dev-credentials endpoint had two auth gaps: a broken Vercel-only fallback and private pages served to everyone. Fixed both. Swing got text chat on the lobby. Quotestreak's art library was 45 percent full; backfilled 37 covers via iTunes. Curvely's color picker crashed on edge cases. Healstack's tab bar was stuck in an overlay eating sheet space; moved it to a safe-area inset. Curbside finally looks like an app: icon built, macOS UX swept, web icons deployed.

Epiphany's broker sync had died silently in production - SnapTrade consumer key was stale from the Vercel migration. Rotated it, added self-healing, and pulled real cost basis and activity.

Nimble's landing is the iPhone frame now. Mac had a self-update check Apple rejected, so I ripped out the whole thing and uploaded the fix.

Apps: Eight apps: seven-app sprint, Bookrank read-aloud (iOS + web), Epiphany broker sync live, direction finalized.

## Wednesday (evening)

Shipped keyrate and adopted Jaybulb tokens across the fleet; added accounts and leaderboard to Quotestreak with web sign-in, reversed Bookrank to a product, and gave it read-aloud (iOS via AVSpeechSynthesizer, web via chapter picker). Fixed Healstack's macOS sidebar, debugged a stale-simulator issue in screenshot tests, and routed branded auth emails through the authmail Worker. Nimble's macOS went back in after removing the self-update check. The broker sync was the real blocker: the old SnapTrade key was hitting the 50-subrequest limit on every sync, so I memoized the calls, rotated the key, and pulled real cost basis and activities tonight. Toroid's macOS 1.0 had a shutdown gap; added NSApplicationDelegate to quit on window close per Guideline 4 and resubmitted to review.

## Thursday

Tuned Homeward's landing page to match Bookrank's theme: swapped the yellow bulb accent for the blue one, switched the buttons to pills with a hover lift effect, tightened up the headline scale, and added a subtle tint for dark mode. Pulled a stray CSS brace too. Built and shipped to Cloudflare Pages.
