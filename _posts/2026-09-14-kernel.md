---
layout: post
title: "Kernel"
date: 2026-09-14 12:00:00 -0700
categories: journal daily
---

{% include headers/2026-09-14-kernel.svg %}

{% include graphs/2026-09-14-kernel.svg %}

Pushed Joshua Tree real semver forward with a live Terminal inside the GUI, Apps folder with 8-icon dock and restorable Trash, and proper per-task memory isolation. Filesystem layer got real: fat16, ramfs, block devices, VFS. Boosted the GUI to 1920x1080 with antialiased DejaVu Sans Mono, live weather from Open-Meteo, wind swaying the tree. Fixed mobile tap-to-click and squashed font/corner/cursor rendering bugs. Changed license to Apache 2.0, rewrote whitepaper, gave landing page real content, wrangler deploy working.

Debugged Turing by asking it real questions and finding out why it answered wrong. FAQ matcher was only 3/16 on paraphrases; rebuilt with semantic matching and jumped to 13/16 zero false positives, pre-commit gated. Made Samantha callable from other apps via OpenAI chat shape. Fixed rate limiting by caching slow paths: 0.93s to 0.04s on repeats. Harvested 183 real instruction/response pairs from fleet git history, rebuilt training data but deferred retrain due to low memory.

Gave Joshua Tree a real Settings app off the Apple menu, with wind animation and dock icon size as live toggles that save straight to a settings file on the filesystem and reload automatically on boot. Verified it with a full round trip: wrote the settings, listed the file back, and the byte count matched exactly.

Launched a background retrain on the 985-chunk dataset (up from 55) with harvested instruction pairs tonight at 01:22; training runs asynchronously, results pending.

**Apps**: joshuatree, turing.
