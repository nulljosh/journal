---
layout: post
title: "Scale"
date: 2026-09-11 12:00:00 -0700
categories: journal quarterly
---

{% include headers/2026-09-11-scale.svg %}

{% include graphs/2026-09-11-scale.svg %}

## Monday

Fixed a broker sync loop in Epiphany burning through Workers quota, the dependency was recreating on every sync, 480k burned since Friday. Pulled the problematic line from useVisibilityPolling and deployed to Cloudflare Workers. Added a lost-cat listing to Homeward for Vinney, an orange tabby, last seen in Brookswood/Langley.

## Sep 8

Flipped five repos to public, finished icon fixes on Lucarne and Plain, and deployed Epiphany's broker-sync fix. Received the CRA Business Number and submitted it to App Store Connect, unblocking iOS revenue. The Duolingo breakthrough came through the React fiber: correct moves sit in the puzzle state, no board scanning needed, just memory reads and clicks, and the language solver reads answer pairs the same way, now verifying live against real Duolingo lessons; bundled js-chess-engine for the match node, found the live FEN in a useRef, and beat the bot in game one, unlocking the next league rank. The Greek course ran unattended through dozens of lessons, its study ledger growing past a thousand entries, after a tree-rendering timeout fix let it resume correctly from checkpoint. Ported the chess work into Seamark, a chart library on npm, extracting coordinate logic and DOM traversal as reusable modules, and deployed the solver landing page to pwnlingo.heyitsmejosh.com.

## Sep 9

The language loop ran unattended overnight across Zulu, Portuguese, and Romanian with a Playwright chess runner driving real mouse clicks through puzzles via live position lookups, zero misses despite bot losses; the engine got rewritten from scratch, a single Playwright runner replacing three loops, a Chrome extension, and a file server, under four hundred lines now. A live hit-trace prune deleted over a hundred dead wrapper layers, cutting code from 21.6k to 19k lines while preserving untraced geometry. Epiphany iOS got approved that morning, paid Daily Brief live, unblocking revenue since the Paid Apps Agreement activated, and got prepped further with a synced splash icon and prefetched Markets drawer news. Built and shipped Co-Stanza, a poetry network with iOS, macOS, and Kotlin ports on Supabase, and fixed voxprint's landing page, tripwire's subrequests, a Curbfind trailing-slash 404 loop, and Sparkjar's iOS comment posting and pagination. Polished the Pwnlingo status app end to end: renamed it, swapped the track dropdown for icon buttons, added lesson counts and a misses counter, wired a live track-switch endpoint, and corrected stale chess stats to 1,700 puzzles overnight with zero misses.

## Sep 10

Paid Apps shipped Voxprint at $1, NYC Survive with icons fixed, and Epiphany Premium via StoreKit 2; built `/launch` for Product Hunt across 12 apps and fixed Sparkjar's auth, asc CLI metadata, and Homeward's mark-found. Wordroot's macOS appeal won and the Duolingo solver evolved into a native iOS and macOS SwiftUI app with an HTTP bridge so mobile can poll progress over LAN; fixed the cheap-fix skill to enforce real delegation to the local model instead of silently writing fixes myself. Shipped Windgate to the App Store, hardened Pwnlingo's runner with crash fixes and watchdog timers, added canvas editing to Blockframe, removed redundant h1s from 16 landing pages, and fixed Epiphany's news drawer bug that was doubling latency past the 15s iOS timeout. Built launch kits for Litigate, Sparkjar, Blockframe, and Doorstock, got Curbfind App Store approval, and scheduled thirteen apps for Product Hunt starting the 11th. Tried Codex CLI for the first time: roost lazy-loads routes, fieldbook got a related-fields sidebar and PDF printing, weather shows hourly forecasts, and Pwnlingo gained full i18n, auto-advance, and menu bar uptime tracking, while Apple rejected a late submission on an automated binary check with no reason given yet.

## Friday (2026-09-11)

Unattended runner had two critical bugs overnight (aria-disabled, lesson-end detection) plus a silent XP counter bug; fixed critical Pwnlingo bugs (SMAppService registration, MenuBarLabel auto-start trigger, language menu state, crash recovery) and verified Cloudflare Web Analytics live across the fleet, then wired social sign-in across the fleet with all four providers to co-stanza, Apple/Google to roost and healstack, plus Face ID and password reset scattered across sparkjar, quotestreak, lexly, bookrank, and homeward on both platforms; spent the afternoon polishing the fleet's landing pages by trimming oversized hero headers on eleven apps, migrating nyc off Vercel onto Cloudflare Workers, removing duplicate device-frame text, shipping the portfolio redesign with the d3-force project graph, and patching twelve repos after discovering half the app previews were dark due to CSP headers. Investigated Healstack's rejections and swept App Store Connect status across the fleet; verified Resend and the three sending domains still work live, and added a themed welcome email that now fires alongside signup confirmations, documenting the architecture in a new resend skill for reference. Extracted the mail skill into a new public repo called sieve with full repo standard, then converted it to a cross-platform status app with iOS/macOS native builds (WKWebView wrapper), a web dashboard that fetches run history from a KV-backed /api/runs endpoint, and the skill POSTs summary counts after each run; gave every repo baseline docs (CONTRIBUTING.md to 41, LICENSE and SECURITY.md to four, WHITEPAPER.md to secretary) and cleaned up the skills folder, consolidating the duplicate wrap-up skill and adding platform-parity-check for cross-platform audits. Restarted the Pwnlingo tunnel (error 1033 had killed it) and fixed a menu bar bug where the label only checked for heartbeat timeout but not runner unreachability, so when the Duolingo bot lost connection it froze showing the last XP number instead of flipping to the warning icon; added animated XP ticker with content transition so the number rolls like an odometer when it updates; fixed a shell annoyance that was tripping me every day (the ls aliases were missing --icons=always, so paths got consumed as flag values and broke) and slimmed 17 skill files in half, cutting redundant examples while keeping every command and gotcha. Polished the menu bar further with a pulse effect on the warning icon and cross-fade transitions between board/prompt/screenshot panels, then discovered a rebuild gotcha after creating two orphaned Pwnlingo processes by building to /tmp instead of the fixed launchd-managed path; rebuilt correctly and confirmed exactly one instance running.

## Apps

Epiphany: quota burn fixed, iOS paid tier live, Premium via StoreKit 2, business number unblocked revenue. Five repos flipped public. Co-Stanza shipped with auth. Duolingo/Pwnlingo bot: engine rewritten, native SwiftUI app, i18n, zero-miss chess, unattended overnight runs across three languages. Seamark: grid clustering engine, npm publish. Monetization: Voxprint $1 paywall, NYC Survive approved. Windgate shipped to App Store. Blockframe canvas editing. Wordroot appeal won. Social sign-in live across nine apps. Fleet landing pages trimmed and standardized. Portfolio redesigned with Jaybulb glass design and d3-force interactive project graph with hover-to-preview. Sieve: skill plus cross-platform status app, iOS/macOS via WKWebView wrapper, web dashboard fetches KV-backed run history, /mail POSTs summary counts after each run.
