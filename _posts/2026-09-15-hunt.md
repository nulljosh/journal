---
layout: post
title: "Hunt"
date: 2026-09-15 12:00:00 -0700
categories: journal daily
---

{% include headers/2026-09-15-hunt.svg %}

{% include graphs/2026-09-15-hunt.svg %}

## Sunday

Overnight on the kernel, fixed two security bugs and a CI false-fail, shipped JPEG decoder, ported NE2000 driver for real networking, and wired 15 regression tests into CI. Multi-window landed with click-to-focus and z-order; also locked down hardware monetization thesis (free OS, revenue via dev-kit/voice/consulting) and trimmed paging table trackers for real memory win. Simplified the portfolio site to plain text: stripped the d3.js force-directed graph and all scroll-fade animations, went paulgraham.com-style instead, shrunk index.html from 437 to 242 lines. Fixed the poetry network's demo iframe render delay (it was waiting on an auth check before routing, half a second of blank screen); now it routes immediately on load and re-routes when the auth listener resolves, so first paint is instant.

## Monday

Pushed checkpoint: skills audit, icon-audit/fleet-beautify, tripwire auto-fix-PR, costanza, chart/banner on landing, CI flake root-caused, repo-cleanup (55 legit, 2 forks); fixed restart bug, Escape trapping, scripts, SVGs, node_modules, Weather Vancouver, headline link, Notes tagline. Caught subagent falsely reporting local commits as "pushed" (typewriter feature unmentioned); main pushed real, reinforces verify-before-declaring. Shipped: mouse wheel in Apps/Launchpad, dynamic typewriter with static "Introducing" and animated app-name, docs, fixed H1 contrast, removed changelog; attempted Cloudflare purge (DNS scope gap). Found "stale cache" was real bug: static HTML never updated when dynamic JS shipped; fixed by syncing to JS default. Ending at usage limit.

## Tuesday

Fixed Conveyer's background monitor to launch on login; investigated tripwire's CLOUDFLARE_API_TOKEN issue and the kernel's notification panel (now translates raw boot-log lines to plain text for test grepping). Cleaned up the portfolio's file structure: removed Xcode projects from version control, consolidated PWA assets, documented directories. Late evening hardening the landing page: rooted a CI flake (kernel events racing QEMU on slow runners), added typewriter headline effect, removed changelog. Finally traced the H1 fade bug back to `updateHeroRecede()` computing scroll progress as `window.scrollY / heroHeight`, but since the hero sits below a full-height demo section, scrollY exceeded heroHeight when the hero came into view, pinning it at 15% opacity; fixed by measuring from the hero's own offsetTop instead.

**Apps**: joshuatree, conveyer, tripwire, costanza.
