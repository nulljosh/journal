---
layout: post
title: "Hunt"
date: 2026-09-15 12:00:00 -0700
categories: journal daily
---

{% include headers/2026-09-15-hunt.svg %}

{% include graphs/2026-09-15-hunt.svg %}

## Sunday

Overnight on the kernel, fixed two security bugs and a CI false-fail, shipped JPEG decoder, ported NE2000 driver for real networking, and wired 15 regression tests into CI. Multi-window landed with click-to-focus and z-order; also locked down hardware monetization thesis (free OS, revenue via dev-kit/voice/consulting) and trimmed paging table trackers for real memory win. Simplified the portfolio site to plain text: stripped the d3.js force-directed graph and all scroll-fade animations, went paulgraham.com-style instead, shrunk index.html from 437 to 242 lines. Fixed the poetry network's demo iframe render delay (it was waiting on an auth check before routing, half a second of blank screen); now it routes immediately on load and re-routes when the auth listener resolves, so first paint is instant.

## Monday

Pushed checkpoint pass: skills audit deleting fifteen duplicates, refactored icon-audit and fleet-beautify, validated tripwire's auto-fix-PR feature (it's live), pinned down costanza's homepage, landed interactive progress chart and dynamic banner on landing page. Live-browser QA identified four follow-ups (Mail blank render, wrong dock-icon launches, Vancouver hardcode unfixed, heap-ceiling question), closed stale GitHub issue, fixed CI flake in editor_qa.py (keystroke buffer race, root cause inside expect() helper), and ran fleet repo-cleanup audit (55 repos legit, 2 forks pending). Late: fixed landing page restart bug (stale-timestamp, 3rd attempt works), fixed Escape key trapping (wrong DOM), organized scripts into tools/, regenerated SVGs, stopped tracking node_modules, deleted bogus tags, finally fixed Weather's Vancouver hardcode, fixed headline link disappearing, replaced Notes demo tagline, and released version covering batch.

## Tuesday

Fixed Conveyer's background monitor to launch on login; investigated tripwire's missing CLOUDFLARE_API_TOKEN issue from 2026-09-14 (infra not code). The kernel's notification panel now translates raw boot-log lines to plain text, keeping regression tests grepping the raw strings as-is. Picked a Raspberry Pi 5 8GB for hardware learning and tested the wikipedia-draft skill across the portfolio. Also cleaned up the portfolio's file structure: removed Xcode projects from version control (just project.yml stays, generated stuff is local), consolidated PWA assets into a dedicated folder, and documented what each directory does (redirect stubs, old theme backups, dev-only wrappers).

**Apps**: joshuatree, conveyer, tripwire, costanza.
