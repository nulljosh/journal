---
layout: post
title: "Network"
date: 2026-09-09 12:00:00 -0700
categories: journal daily
---

{% include headers/2026-09-09-network.svg %}

{% include graphs/2026-09-09-network.svg %}

## Tuesday

Built and shipped Co-Stanza, a Svbtle-shaped poetry network with iOS, macOS and Kotlin ports all using Supabase with row-level security and full auth. Cleaned up voxprint's repo (shrinking it from 1.5 GB to 518 MB) and fixed tripwire's subrequest limit issues. Fixed a funny Curbfind bug: the landing page linked to /app without a trailing slash, so Cloudflare Pages 404'd and fell back to the same page. Clicking the button just looked like it redirected to itself.

## Wednesday

Paid Apps Agreement shipped: Voxprint with a $1 paywall, NYC Survive with fixed icons, Epiphany Premium tier via StoreKit 2 submitted, all three declaring exempt encryption. Built a `/launch` skill for Product Hunt kits, ran it across 12 apps (Voxprint going live Sep 10, others staged), and tested $0.99 pricing on nine apps before reverting to free. NYC Survive iOS got approved; added the App Store link and deployed to Cloudflare. Debugged Sparkjar's Supabase auth config, which still pointed to the old spark.heyitsmejosh.com domain; fixed it to sparkjar.heyitsmejosh.com via the Management API so verification emails would redirect correctly.

## Apps

Co-Stanza poetry network launched with auth across all platforms. Monetization shipping: Voxprint with $1 paywall (3 free transcriptions), NYC Survive with fixed icons, Epiphany Premium tier via StoreKit 2 submitted. Fixed Swing's 18+ gate button, which looked broken but wasn't: the click worked, just the CSS rule `#gate[hidden] { display: none; }` was missing, so hiding the overlay never rendered.
