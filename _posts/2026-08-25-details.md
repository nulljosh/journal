---
layout: post
title: "Details"
date: 2026-08-25 12:00:00 -0700
categories: journal daily
---

{% include headers/2026-08-25-details.svg %}

Cleared the Apple Notes backlog into nine repo roadmaps and three wiki pages. An agent briefly overwrote nine roadmap.md files instead of appending, destroying 2,099 lines, but all nine were recovered and reconciled. Fixed a phone bug in Epiphany where the situation panel mounted open on login. Added a Safari Smart App Banner to every live app: one `<meta>` tag iOS renders as a download link. Trimmed the portfolio intro. Added haptic feedback to Lexly's answer checks. Nimble's WhatsNewSheet now reads from the bundle instead of being hardcoded. Bookrank's `sync-summaries.sh` had silently replaced a large summary with a stale one; both copy paths now guard against overwrites under 80% file size. Confirmed Epiphany's SnapTrade billing and key status. Marked Bookrank's AI in Business as partial. Removed Uprighty after verifying its work was rescued.

Late evening on Epiphany: fixed three bugs in the mobile search bar (hidden under hamburger), avatar saves (blob shim lacking KV), and traffic API (inventing fake incidents). Migrated web from Vercel to Cloudflare Workers and shipped the iOS build for review. Test suite passes.

Restyled the portfolio with EB Garamond serif and lovefrom.com design tokens. Ditched the rounded corners, shadows, and pixel avatar - the whole point was restraint. Fixed a script bug that would've crashed on page load.

**Apps:** Epiphany (web on Cloudflare Workers, iOS submitted, avatar/traffic fixes), Lexly (iOS with haptic feedback), Talli (web updated), Bookrank (web updated, inventory guard added), Litigate (web updated), Inkpress (web updated), Curvely (web updated), Wiretext (web updated), Voxprint (web updated), Sparkjar (web updated), Portfolio (restyled). Ten live apps now have Smart App Banners. Nine submissions waiting on review.

## Tuesday (evening wrap)

Checked the two pending rejections. NYC Survive was flagged on screenshot quality - no code fix needed, just new marketing images. Sparkjar's decode-error rejection turned out to be from Apple reviewing a stale build from August 18 (fix landed August 22). Rebuilt Sparkjar and fixed a workflow bug where VERSION defaulted to empty instead of "1.0".

**Usage script:** Enhanced `~/.claude/scripts/usage.sh` to show reset times in local time (e.g., "resets Mon 22:49").
