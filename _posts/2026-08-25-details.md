---
layout: post
title: "Details"
date: 2026-08-25 12:00:00 -0700
categories: journal daily
---

{% include headers/2026-08-25-details.svg %}

Cleared the whole Apple Notes backlog into nine repo roadmaps and three wiki pages, then deleted all nineteen items. A mishap during the filing: an agent overwritten nine roadmap.md files instead of appending, destroying 2,099 lines (sparkjar 738, lexly 420, bookrank 252, talli 161, epiphany 146, wordroot 136, nimble 110, nulljosh.github.io 72, litigate 64). The blunder was caught and all nine were recovered by restoring from the parent commit with the Apple Notes section re-appended, verified append-only against the pre-clobber originals, and open-item counts reconciled. Fixed a phone bug in Epiphany where the situation panel mounted open on login and covered the screen. Added a Safari Smart App Banner to every live app's landing page: one `<meta>` tag that iOS renders as a download link. Trimmed the portfolio intro from four paragraphs to business-card length. Added haptic feedback to Lexly's answer checks. Nimble's WhatsNewSheet was hardcoded (a version that never shipped) and now reads CFBundleShortVersionString from the bundle so it stays current. Bookrank's `sync-summaries.sh` had already silently replaced a 498KB summary with a stale 304KB one. Both copy paths now route through a `safe_cp` guard that refuses overwrites under 80% of the existing file size. Confirmed Epiphany's SnapTrade billing was paid and the key never disabled. Marked Bookrank's AI in Business chapter as partial instead of re-borrowing it, the site now reads "ch. 1-10; ch. 11-14 unavailable". Removed Uprighty from ~/Documents/Code after verifying it was a diverged clone of bookrank holding unique books work already rescued to origin/books-wip and contained only one commit nowhere on the remote.

**Apps:** Epiphany (web updated, SnapTrade confirmed live), Lexly (iOS with haptic feedback), Talli (web updated), Bookrank (web updated, inventory guard added), Litigate (web updated), Inkpress (web updated), Curvely (web updated), Wiretext (web updated), Voxprint (web updated), Sparkjar (web updated). Ten live apps now have Smart App Banners. Nine submissions waiting on review.
