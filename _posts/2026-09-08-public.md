---
layout: post
title: "Public"
date: 2026-09-08 12:00:00 -0700
categories: journal monthly
---

{% include headers/2026-09-08-public.svg %}

{% include graphs/2026-09-08-public.svg %}

## Monday

Fixed a broker sync loop in Epiphany burning through Workers quota. The dependency was recreating on every sync, 480k burned since Friday. Pulled the problematic line from useVisibilityPolling and deployed to Cloudflare Workers. Added a lost-cat listing to Homeward for Vinney, an orange tabby, last seen in Brookswood/Langley.

## Tuesday

Flipped five repos to public and finished icon fixes on Lucarne and Plain. Deployed Epiphany's broker-sync fix and realized Wrangler OAuth lives in ~/.wrangler/config/default.toml, not secrets.fish. Received the CRA Business Number and submitted it to App Store Connect, unblocking iOS revenue. Swing's lobby works fine; it just needs camera permission first, then waits for a second person to join.

## Duolingo

The breakthrough came through the React fiber. Correct moves sit in the puzzle state; no board scanning, just memory reads and clicks. Solved chess puzzles all correct. The language solver reads answer pairs the same way and now verifies live against real Duolingo lessons through langBoot.js. Bundled js-chess-engine for the match node, found the live FEN in a useRef, and beat the bot in game one, unlocking the next league rank. Improved the engine with Lasker's principle: when you see a good move, ask one level deeper, and take the deeper answer if it disagrees.

The Greek course ran unattended through dozens of lessons with the private study ledger growing past a thousand entries. Fixed langBoot's tree-rendering timeout to handle slower page loads, then restarted the loop and it resumed correctly from its checkpoint. Ported the chess work into Seamark, a chart library on npm. Extracted coordinate logic and DOM traversal as reusable modules, added tests, published on npm. Built a live-solving widget demo for the Seamark landing page, fixed the hero phone mockup, and gated the demos to render only inside the device frame. Deployed the solver landing page to Cloudflare Pages at pwnlingo.heyitsmejosh.com with updated README on chess, language, and gaps.

## Wednesday

The language loop ran unattended overnight across Zulu, Portuguese, and Romanian. A Playwright chess runner drove real mouse clicks through puzzles by looking up answers from the live position data; zero misses once that pattern became clear, though bot matches all lost. The engine got rewritten from scratch: a single Playwright runner replaced the three loops, the Chrome extension, and the file server, with the new solver under four hundred lines while the old code lives as math-legacy.js on a separate branch where another agent is live-tracing it to prune dead paths. Seamark gained new retrieval methods and tests with publish queued.

## Apps

Epiphany quota burn fixed. Five repos now public. Duolingo bot: engine rewritten, language courses live overnight. Seamark pending publish.
