---
layout: post
title: "Income"
date: 2026-08-23 12:00:00 -0700
categories: journal daily
---

{% include headers/2026-08-23-income.svg %}

## Sunday (August 23)

Fixed the Talli web home tab to show correct income by reading from config instead of just the provincial PWD half; now $1,650 with chart, 2026 total $11,550. Removed abraham and code-meta, rebuilt homeward iOS, and added two real Gastown listings. Submitted Sparkjar iOS and both BCGD platforms (four apps now waiting review); tried deleting the three stray app records but they're stuck in REJECTED state and need an Apple phone call.

Also rewrote Quotable's iOS app from a WebView wrapper to native SwiftUI, expanded the quote bank from 193 to 272 entries, added ~1500 assertion checks, fixed UI bugs (button tint and a no-purple badge), and shipped it as Quotestreak in review. Discovered `asc validate` missed two real blockers that only showed up during submit (iPad screenshots and pricing), so those got fixed and resubmitted; macOS is still the WKWebView version.

## Apps

Talli (iOS shipped, web live), Lexly (iOS shipped, macOS in review), Roost (web live), Homeward (web live, iOS builds now), Bookrank (iOS/macOS in review or live), Curvely (iOS shipped, in review), Sparkjar (iOS/macOS in review), BCGD (iOS/macOS in review), Quotable (iOS Quotestreak in review).
