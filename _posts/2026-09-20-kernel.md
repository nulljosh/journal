---
layout: post
title: "Kernel"
date: 2026-09-20 12:00:00 -0700
categories: journal quarterly
---

{% include headers/2026-09-20-kernel.svg %}

{% include graphs/2026-09-20-kernel.svg %}

I restarted Turing and found small models memorize style but not facts; brain lookup jumped scores from 47 to 57 on knowledge questions. Fixed brain's Worker and widened coverage. Samantha got hands: 13 tools (open apps, search, read pages, screenshot, clipboard, volume, battery, speak, read files, make logos), plain commands via regex router, multi-step asks through 1.7B Qwen via Ollama. She built a 28-layer logo live in Pixelmator by turning seven dials. An article reader grounds her answers with a page lookup and grounding check; confidently wrong answers dropped from 18 to 4. Actions eval hit 54 of 54. README and landing page now show real transcripts, her logo, and a chat box replaying recordings. Voice retrain on 985 chunks is running locally. Found: global core.hooksPath was breaking the pre-commit hook. Roadmap lists the ship gate: knowledge to 60 of 65 zero-wrong, personal tools, music, browser tabs, real harness, her own tool model. A blocked project returned censorship articles; now corrects them. Craigslist skill tested on a Canon printer. The landing page rebuilt around her chat as the main demo, typing by itself idle then stopping when touched, below it release-style results (13 tools, 54/54, 57/65, confident wrongs down to 4) and docs coverage at 36% with SECURITY.md the missing piece; a bar-scaling bug caught and fixed before deploy.

Joshua Tree dominated after: a kernel with real interrupts, paging, task switching, disks, filesystems, and a file browser; landing demo boots live in the browser. Polish pass: 20 fixes including Mac-style clock, redrawn icons, 1984-style boot hello, Apple menu. Calendar with persistent events, 73K dates verified, Settings toggles, real Trash, glass menu bar, landing page with progress graph.

Saturday shipped Mail, Contacts, Calculator, wallpapers, weather particles, real location lookup. Bugs: notification font was cursor text underneath; dock magnify stacked problems; weather queried wrong city; network driver ran one packet behind. Added real syscall gate, clean teardown, VMware support, full docs, CI on push.

Landing header move broke boot logo position; stopped CI on network hiccups, refused multi-window fakes. Business thesis: OS free, money from hardware, dev kit, voice, consulting.

Fixed two security bugs, shipped JPEG decoder, ported network driver, wired 15 regression tests. Multi-window with click-to-focus, memory optimizations. Backfilled 19 missing tags, automated tagging. Landing: wheel scroll, typewriter headline, rounded demo. Static page bug never matched script render; caught subagent mislabeling local commits as pushed.

Midweek traced purple boot logo to a drawing function ignoring color. Fixed garbage text in Notes; swapped pink/purple for silver/black/white. Tree wallpaper flashed on startup because fix ran everywhere except one frame. Codex joined with shared instructions and review. Landing drew from work queue. Friday merged Apps folder redraw, shipped release, added live reload, recorded headless runs, flattened icon; screen repaint on every input is the blocker.

This weekend brought glossy tiles, syscalls, graphs, lazy loading, the back buffer. Pinning Stocks triggered a cascade: gibberish terminal, header sync issues, red CI, check failures, lag, dead weather. I spent a morning on it, frustrated. Sunday was better: the icon, a Weather window, a save test, and the landing page done, six pieces left before 1.0. I restored the satellite wallpaper's color and took the landing from papery to black and white, the demo as its fullscreen hero. A new Pixelmator logo tool recreated my dad's Best Choice logo in six seconds.

Curvely got Desmos parity (draggable sliders, implicit equations, fullscreen graph); iOS/macOS submitted. Roost: non-overlapping labels; Dream: delete-all; books became badges; Hormuz: live geopolitics, glass map. Conveyer fixed eight skill bugs, Gato's voice fixed, Pwnlingo's Korean fixed, Settings priority.

Portfolio design tokens weren't linked; hairlines showed yellow until fixed. Homepage cut to five picks, full list moved to own page. Sparkjar iOS cleared review, web got sign-in menu and infinite scroll. Brain moved memory viewer. Lexly got Unit 1 examples.

Main drive filled; borrowed 2TB LaCie to host models. Lucarne's Mac rejection appeal filed. Then the name: Madobe stuck (Japanese for by the window).

Pwnlingo stalled; bounded retries, fixed matching; 46 tests pass. Fleet committed to money: eight apps to 99 cents (web free), MONEY.md in every repo, 23 launch kits, Voxprint paywall live. Product Hunt had twelve launches.

Tonight the fleet got cleaned: fifty-plus repos with launch copy fixes, CI tuning, progress graphs, and MONEY.md standardized. Hormuz shipped with the map finally visible, dark-mode inverted, status live from IMF PortWatch, the strait closed since February with five daily vessels.

Talli got a fix straight from a user: they had read every message and the badge still said eight. It only cleared when you tapped each message open, so now opening Messages marks them all read, on the phone and the web. The update is in review, and Talli stays free on purpose.

This evening Joshua Tree got serious passes. The wishlist went into the roadmap: landing page now black-and-white with terracotta (live, auto-theme); demo frames above headline as fullscreen hero. Repo tidied: docs moved, kernel binary removed and built at deploy-time, pre-push hook refuses reds. Monitor app moved to its own private repo, shows version and open count. Boot logo seams fixed; follow-up PR redraws tree and pointer at full resolution. Kernel boots on real video cards via Limine; desktop worked on two non-emulator cards in QEMU and boots as USB. Activity Monitor PR fixed. Run-programs and Chat PRs in merge queue. Domain picked: joshuatreeos.com (Joshua buys Monday). Honest verdict: not 1.0 yet; beta Monday, then final. Real 1.0 gate: fix lag, real PC USB boot with photo, user profiles and sign-in, typography QA, test per app, Mac icons, Settings location. Drivers next. Self grade: B plus. Overnight: PRs #78 (logo and pointer smooth) and #79 (landing demo and headline share first screen, Full screen button, no auto scroll) merged. Shell script landed five more: #76 (USB boot, real video cards), #75 (menu bar app, roadmap/docs at 100%), plus #64, #65, #62. Beta ships Monday, final Tuesday. C stays; Rust for new drivers after 1.0.

Late evening brought the fleet docs pass. All 54 repos got docs/ARCHITECTURE.md at 95%+ coverage. Docs page went live at heyitsmejosh.com/docs.html; progress graphs now show "% documented"; 22 app footers link to it. Sonnet verified accuracy; Opus and Sonnet rewrote intros into plain English. /docs-sweep skill expanded. Key lesson: I fanned out 7 subagents, got called out, and added a rule to never do that again. One at a time, sequentially, saves usage.

I opened my own transcription app on my phone and it would not even load, just an error about the speech model. Turned out that if the app got interrupted once while saving the model, it kept loading the half saved copy forever, and Apple approved it because a fresh install works fine. It heals itself now, and a test now breaks the model on purpose and makes the app transcribe real speech before anything ships. I also ripped out the Pro upgrade so it is just a dollar, drew a new icon, and gave it a better name. Voxprint is now Notate. I tried eighteen other plain words first and every one was taken. Both updates are in review. Next I want the small model built into the app so nobody waits on a download the first time.
