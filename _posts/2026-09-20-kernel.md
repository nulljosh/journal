---
layout: post
title: "Kernel"
date: 2026-09-20 12:00:00 -0700
categories: journal quarterly
---

{% include headers/2026-09-20-kernel.svg %}

{% include graphs/2026-09-20-kernel.svg %}

Turing showed small models memorize style not facts; brain jumped 47 to 57. Samantha got hands (13 tools: open-apps to logo-maker, regex router, 1.7B Qwen); article reader dropped wrongs 18 to 4, actions 54/54. Built a 28-layer logo in Pixelmator. Found core.hooksPath broke pre-commit. Roadmap: knowledge to 60/65, personal tools, music, tabs, real harness. Landing rebuilt as demo with idle typing and live results (57/65).

Joshua Tree dominated after: kernel with real interrupts, paging, task switching, disks, filesystems, file browser; landing boots live in browser. Polish: Mac clock, redrawn icons, boot hello, Apple menu; calendar with persistent events (73K dates verified), Settings, real Trash, glass menu bar. Saturday shipped Mail, Contacts, Calculator, wallpapers, weather with noted bugs in notification font, dock magnify, weather lookup, network timing. Added syscall gate, clean teardown, VMware, docs, CI. Business thesis: OS free, money from hardware, dev kit, voice, consulting.

Fixed two security bugs, shipped JPEG decoder, ported network driver with 15 regression tests; added multi-window click-to-focus, memory optimizations, 19 tag backfills. Landing got wheel scroll, typewriter headline, rounded demo, static page bug fixed.

Midweek traced purple boot logo to a drawing function ignoring color; fixed garbage text in Notes. Tree wallpaper flash on startup pending a frame fix. Friday merged Apps redraw, shipped release, live reload, headless runs, flattened icon; screen repaint on every input remains the blocker.

This weekend brought glossy tiles, syscalls, graphs, lazy loading, back buffer; pinning Stocks triggered a cascade of gibberish terminal, header sync failures, red CI, and dead weather, frustrating. Sunday finished the icon, Weather window, save test, landing; six pieces left before 1.0. Restored satellite wallpaper, took landing from papery to black and white; logo tool recreated my dad's logo in six seconds.

Curvely got Desmos parity (sliders, implicit equations); iOS/macOS submitted. Fleet updates: Roost labels, Dream delete-all, books badges, Hormuz live geopolitics, Conveyer/Gato/Pwnlingo fixes. Portfolio design tokens weren't linked; hairlines showed yellow until fixed. Homepage cut to five picks, full list on own page. Sparkjar iOS cleared review, web got sign-in menu and infinite scroll. Brain moved memory viewer, Lexly got Unit 1 examples.

Main drive filled; borrowed 2TB LaCie to host models. Lucarne's Mac rejection appeal filed. Then the name: Madobe stuck (Japanese for by the window).

Pwnlingo stalled; bounded retries, fixed matching; 46 tests pass. Fleet committed to money: eight apps to 99 cents (web free), MONEY.md in every repo, 23 launch kits, Voxprint paywall live. Product Hunt had twelve launches.

Tonight the fleet got cleaned: fifty-plus repos with launch copy fixes, CI tuning, progress graphs, and MONEY.md standardized. Hormuz shipped with the map finally visible, dark-mode inverted, status live from IMF PortWatch, the strait closed since February with five daily vessels.

Talli got a fix straight from a user: they had read every message and the badge still said eight. It only cleared when you tapped each message open, so now opening Messages marks them all read, on the phone and the web. The update is in review, and Talli stays free on purpose.

This evening Joshua Tree got serious passes. Wishlist into roadmap: landing black-and-white with terracotta (auto-theme), demo as fullscreen hero. Repo tidied: docs moved, kernel binary at deploy-time, pre-push hook refuses reds; Monitor app spun out. Boot logo fixed; kernel boots on real video cards via Limine and USB. Domain: joshuatreeos.com. Gate: fix lag, USB boot, sign-in, typography QA, Mac icons. Grade: B plus. PRs merged: #78/#79 (logo, landing hero, fullscreen), #76 (USB/video), #75 (menu bar, docs 100%), #64/#65/#62. C stays; Rust for new drivers post-1.0.

Late evening brought the fleet docs pass: all 54 repos got docs/ARCHITECTURE.md at 95%+ coverage; docs page went live at heyitsmejosh.com/docs.html with progress graphs and 22 app footer links. Sonnet verified accuracy, Opus rewrote intros into plain English. Key lesson: parallel subagents burned usage; now sequential.

I opened my transcription app and it would not load due to a corrupted model save. If an interrupt happened mid-save, the app kept loading the broken copy forever; Apple approved because fresh installs work. Fixed with self-healing and a test that breaks the model on purpose and transcribes real speech before shipping. Renamed it to Notate (18 other words taken), dropped Pro, redrew the icon. Both updates in review. Next: bundle the model inside so no download on first open.

Sunday morning fixed the portfolio docs page phone overflow, where code names pushed beyond screen width. Added /os.html, a live Joshua Tree boot in the browser, linked from the homepage. Joshua Tree PR #81 merged (?full switch for landing); PR #85 open with Portfolio app inside the OS and portfolio mode swapping dock to my nine shipped apps with an idle tour. Notate docs rewritten for the transcription release.

Late Sunday, Samantha grew from 13 to 19 tools; landing became a live demo with Wikipedia lookups via Cloudflare and safety-gated model for unknowns. Knowledge rose to 62 of 65, tool picker at 395/484 on unseen; simultaneous training and eval filled the disk, so training now refuses to start under 6GB free. Toroid shipped: Game of Life on the dock plus Portfolio, Epiphany, Keyrate, Quotes as real apps. Portfolio site root is now the OS booting in portfolio mode; idle demo opens apps live, old homepage moved to classic.html.

Monday fixed the Dock launcher silently failing: Mac app moved to its own repo but still looked four folders up. Added logging, merged window dragging and snapping. One-point-zero: polish and QA. Frame-by-frame headless testing caught bugs forty tests missed: Files crash, Terminal crush, Calendar's last week. Security audit found password in memory post-hash. Disk hit zero mid-run; recovered 64 GB by clearing Xcode DerivedData, caches, watchOS simulator (9 GB), Claude VM bundles (10 GB), and symlinking Steam to LaCie; now keeps 20+ GB headroom. Usage ran out before 1.0 shipped.

Fixed the landing demo spitting HTML on weather: wttr.in serves full pages, demo only checked for degree signs. She asks JSON, capped at 1200 chars per reply. Added caption to her T logo; wrote SECURITY.md. Web_demo.py tests weather, bad places, every chip, idle-reel lines, fails on runaway/raw-page. Caught: complex logo chip never routed. Evals green: 77/77 actions, 77/77 web, zero failures.

Monday brought a paint command to Pixelmator: rebuilds any image from thousands of colored rectangles as editable layers. Mona Lisa emerged as 2997 layers over eight minutes, recognizable but pixelated. Speed gains from placing shapes at creation time and skipping rectangles matching the background. Dad's garage door logo rendered in 94 seconds but looked chunky with mushy text, needing real text layers instead. Landing refreshed with live in-browser painter, new icon, and updated README; target deployment is pixelmator.heyitsmejosh.com.

Nimble's iOS app got rejected for sending queries to an AI service without warning or asking permission. I fixed it by adding a permission prompt on the first search that names the service (Gemma and Qwen on Cloudflare Workers AI, or Anthropic/OpenAI if the user chooses those), with Allow and Don't Allow buttons; declining falls back to Wikipedia and DuckDuckGo. Mirrored the prompt into the Mac version's HUD and Settings, updated privacy policy, confirmed the flow works on a fresh simulator install, and resubmitted. macOS still needs an update to carry the prompt to the live app.
