---
layout: post
title: "Kernel"
date: 2026-09-20 12:00:00 -0700
categories: journal quarterly
---

{% include headers/2026-09-20-kernel.svg %}

{% include graphs/2026-09-20-kernel.svg %}

I restarted Turing, my local model project, and learned small models memorize style but can't learn facts. Pivoting to looking answers up in my notes via brain jumped the score from one correct to 14. I fixed brain's Worker, widened coverage, and noted the hardware ceiling. A blocked project returned censorship articles and model questions got food donations, both fixed. A new Craigslist skill posts across Craigslist, Marketplace, and Kijiji; tested with a Canon printer.

Then Joshua Tree took over: a kernel with real interrupts, paging, task switching, disks, filesystems, and a file browser, and a landing demo that boots the OS live in the browser. A polish pass brought 20 fixes, from a Mac-style clock and redrawn icons to a 1984-style hello on boot and an Apple menu. Calendar came next with persistent events and 73,049 dates verified, plus Settings toggles, a real Trash, a glass menu bar, and a landing page with a progress graph and parallax.

One Saturday I shipped Mail, Contacts, Calculator, day and night wallpaper, weather particles, and real location lookup. The notification panel's font was the cursor smoothing text underneath; the dock's magnify was two stacked problems; weather was asking about the wrong city. The network driver had run one packet behind, hidden until weather made consecutive requests. I added a real syscall gate, clean teardown, VMware mouse support, full documentation, and CI on every push.

Moving the landing header broke the boot logo's position. I stopped CI on network hiccups and refused faking multi-window. The business thesis became a skill: the OS stays free, the money comes from hardware, a dev kit, voice, and consulting.

Next I fixed two security bugs, shipped a JPEG decoder, ported a second network driver, and wired 15 regression tests into CI. Multi-window arrived for real with click to focus and stacking, and trimming the page table trackers saved memory. I backfilled 19 missing tags with their GitHub releases and made tagging automatic. The landing page got wheel scrolling, a typewriter headline, and a rounded demo. A supposed stale cache was a real bug, the static page never matched what the script drew, and I caught a subagent reporting local commits as pushed, so I verify before calling anything done.

Midweek I traced a purple boot logo to a drawing function that ignored the color it was given. I fixed garbage text in Notes and swapped leftover pink and purple for silver, black, and white. The tree wallpaper kept flashing on startup because my fix ran everywhere except the one frame that mattered. Codex joined the project with shared instructions and a review loop, and the landing page now draws its plans from the work queue. By Friday I merged the Apps folder redraw, shipped a release, added a live reload loop and recorded headless test runs, and flattened the landing icon, leaving the screen repainting on every input as the real blocker.

This weekend brought glossy tiles, syscalls, graphs, lazy loading, the back buffer. Pinning Stocks triggered a cascade: gibberish terminal, header sync issues, red CI, check failures, lag, dead weather. I spent a morning on it, frustrated. Sunday was better: the icon, a Weather window, a save test, and the landing page done, six pieces left before 1.0. I restored the satellite wallpaper's color and took the landing from papery to black and white, the demo as its fullscreen hero. A new Pixelmator logo tool recreated my dad's Best Choice logo in six seconds.

Curvely got real Desmos parity: draggable sliders update every equation, hover tracing and implicit equations work. Graph fills the screen with equation list as a floating glass panel; iOS and macOS versions submitted. Roost got non-overlapping labels, Dream got delete-all, two books became badges. Hormuz is new: oil prices, live geopolitics signals, glass map. Conveyer came back: fixed eight skill bugs, added base memory and curriculum. Gato's voice recording got fixed, Pwnlingo's Korean tiles read right, Settings opens in front, sound defaults off.

The portfolio wasn't loading its design tokens, so hairlines showed yellow until I linked the right file. Later the homepage got cut to five fully shipped picks, the full list moved to its own page grouped by kind, and I removed the sync script that kept creating duplicates, fixed stale names, and added Turing, Conveyer, and Hormuz. All 75 links check out live. Co-Stanza's demo now paints instantly. Sparkjar iOS cleared review after weeks stuck in the spam wave, and its web app got a fixed What's New modal, sign in under a profile menu, and infinite scroll. Two Workers fought over brain's domain, so the memory graph viewer moved to brain.heyitsmejosh.com/graph. The feature audit skill records video now, since stills hide animation bugs. Lexly's precalc masterclass got Unit 1 worked examples and a printable copy.

My main drive filled completely, blocking Mail. I deleted stale Ollama models and borrowed a 2TB LaCie from my dad to host the rest, clearing to 27GB free. Lucarne's Mac rejection got an appeal, the support link had been briefly down and the crash was real. Then the name: Fenster too angry, Ikkuna faded, Janela mid, Madobe stuck (Japanese for by the window).

Pwnlingo had stalled for 23 hours, 1,033 cycles with zero XP, silent about why. I bounded its retries, fixed tile matching, dismissed stalled stories, and skipped blacklisted tracks; 46 tests pass. Onboarding went live on 11 apps. I committed the fleet to making money: eight apps moved to 99 cents, still free on the web, a money file in every repo, launch kits for all 23 apps, and Voxprint's paywall live. Product Hunt already had twelve launches live and two scheduled, so the next round starts from that list. The launch skill now works in Codex.

Tonight the fleet got cleaned: fifty-plus repos with launch copy fixes, CI tuning, progress graphs, and MONEY.md standardized. Hormuz shipped with the map finally visible, dark-mode inverted, status live from IMF PortWatch, the strait closed since February with five daily vessels.

Talli got a fix straight from a user: they had read every message and the badge still said eight. It only cleared when you tapped each message open, so now opening Messages marks them all read, on the phone and the web. The update is in review, and Talli stays free on purpose.

This evening Joshua Tree got a serious pass. The wishlist went into the roadmap: landing page now black-and-white with terracotta (live, auto-theme); demo frames above headline as fullscreen hero. Repo tidied: docs moved, kernel binary removed and built at deploy-time, pre-push hook refuses reds. Monitor app moved to its own private repo, shows version and open count. Boot logo seams fixed; follow-up PR redraws tree and pointer at full resolution. Kernel boots on real video cards via Limine; desktop worked on two non-emulator cards in QEMU and boots as USB. Activity Monitor PR fixed. Run-programs and Chat PRs in merge queue. Domain picked: joshuatreeos.com (Joshua buys Monday). Honest verdict: not 1.0 yet; beta this weekend, final in the following days. Real 1.0 gate: fix lag, real PC USB boot with photo, user profiles and sign-in, typography QA, test per app, Mac icons, Settings location. Drivers next. Self grade: B plus.
