---
layout: post
title: "Kernel"
date: 2026-09-20 12:00:00 -0700
categories: journal quarterly
---

{% include headers/2026-09-20-kernel.svg %}

{% include graphs/2026-09-20-kernel.svg %}

Turing showed small models memorize style not facts; brain's knowledge jumped 47 to 57. Samantha got hands (13 tools from open-apps to logo-maker, regex router, 1.7B Qwen for multi-step). Built a 28-layer logo in Pixelmator. Article reader dropped confident wrongs 18 to 4; actions eval 54/54. Found core.hooksPath broke pre-commit. Roadmap: knowledge to 60/65, personal tools, music, tabs, real harness, own model. Landing rebuilt as main demo with idle typing and live results (57/65 showing, SECURITY.md the final piece done).

Joshua Tree dominated after: kernel with real interrupts, paging, task switching, disks, filesystems, file browser; landing boots live in browser. Polish: 20 fixes (Mac clock, redrawn icons, boot hello, Apple menu); calendar with persistent events, 73K dates verified, Settings, real Trash, glass menu bar, landing graph.

Saturday shipped Mail, Contacts, Calculator, wallpapers, weather; bugs in notification font, dock magnify, weather city lookup, network driver timing. Added syscall gate, clean teardown, VMware, docs, CI.

Landing header move broke boot logo position; stopped CI on network hiccups, refused multi-window fakes. Business thesis: OS free, money from hardware, dev kit, voice, consulting.

Fixed two security bugs, shipped JPEG decoder, ported network driver, 15 regression tests; multi-window click-to-focus, memory optimizations, 19 tag backfills. Landing: wheel scroll, typewriter headline, rounded demo; static page bug fixed.

Midweek traced purple boot logo to a drawing function ignoring color; fixed garbage text in Notes, swapped colors. Tree wallpaper flashed on startup; frame fix pending. Codex joined with shared instructions. Friday merged Apps redraw, shipped release, live reload, headless runs, flattened icon; screen repaint on every input is the blocker.

This weekend brought glossy tiles, syscalls, graphs, lazy loading, back buffer. Pinning Stocks triggered a cascade: gibberish terminal, header sync, red CI, lag, dead weather. Spent a morning on it frustrated. Sunday: the icon, Weather window, save test, landing done, six pieces left before 1.0. Restored satellite wallpaper, took landing from papery to black and white. Logo tool recreated my dad's logo in six seconds.

Curvely got Desmos parity (sliders, implicit equations); iOS/macOS submitted. Roost: labels; Dream: delete-all; books: badges; Hormuz: live geopolitics. Conveyer, Gato, Pwnlingo fixes; Settings priority.

Portfolio design tokens weren't linked; hairlines showed yellow until fixed. Homepage cut to five picks, full list moved to own page. Sparkjar iOS cleared review, web got sign-in menu and infinite scroll. Brain moved memory viewer. Lexly got Unit 1 examples.

Main drive filled; borrowed 2TB LaCie to host models. Lucarne's Mac rejection appeal filed. Then the name: Madobe stuck (Japanese for by the window).

Pwnlingo stalled; bounded retries, fixed matching; 46 tests pass. Fleet committed to money: eight apps to 99 cents (web free), MONEY.md in every repo, 23 launch kits, Voxprint paywall live. Product Hunt had twelve launches.

Tonight the fleet got cleaned: fifty-plus repos with launch copy fixes, CI tuning, progress graphs, and MONEY.md standardized. Hormuz shipped with the map finally visible, dark-mode inverted, status live from IMF PortWatch, the strait closed since February with five daily vessels.

Talli got a fix straight from a user: they had read every message and the badge still said eight. It only cleared when you tapped each message open, so now opening Messages marks them all read, on the phone and the web. The update is in review, and Talli stays free on purpose.

This evening Joshua Tree got serious passes. Wishlist into roadmap: landing now black-and-white with terracotta (auto-theme), demo as fullscreen hero. Repo tidied: docs moved, kernel binary built at deploy-time, pre-push hook refuses reds; Monitor app spun out, shows version/count. Boot logo fixed; kernel boots on real video cards via Limine and USB. Domain: joshuatreeos.com. Not 1.0 yet; beta Monday, final Tuesday. Real gate: fix lag, USB boot, sign-in, typography QA, Mac icons. Grade: B plus. Overnight: PRs #78, #79 merged (logo smooth, landing hero, fullscreen button). Five more landed: #76 (USB/video), #75 (menu bar, docs 100%), plus #64, #65, #62. C stays; Rust for new drivers post-1.0.

Late evening brought the fleet docs pass. All 54 repos got docs/ARCHITECTURE.md at 95%+ coverage. Docs page went live at heyitsmejosh.com/docs.html; progress graphs show "% documented"; 22 app footers link to it. Sonnet verified accuracy; Opus rewrote intros into plain English. Key lesson: fanning out 7 subagents burned usage, so now one at a time, sequentially.

I opened my transcription app and it would not load due to a corrupted model save. If an interrupt happened mid-save, the app kept loading the broken copy forever; Apple approved because fresh installs work. Fixed with self-healing and a test that breaks the model on purpose and transcribes real speech before shipping. Dropped Pro, redrew the icon, renamed it to Notate (18 other words were taken). Both updates in review. Next: bundle the model inside so no download on first open.

Sunday morning fixed the portfolio docs page phone overflow, where code names pushed beyond screen width. Added /os.html, a live Joshua Tree boot in the browser, linked from the homepage. Joshua Tree PR #81 merged (?full switch for landing); PR #85 open with Portfolio app inside the OS and portfolio mode swapping dock to my nine shipped apps with an idle tour. Notate docs rewritten for the transcription release.

Late Sunday, Samantha grew from 13 to 19 tools. Landing became a live demo; Wikipedia lookups via Cloudflare; safety-gated model for unknowns. Knowledge 57 to 62 of 65, tool picker scored 395 of 484 on unseen. Simultaneous training and eval filled the disk; now training refuses to start under 6GB free. Toroid shipped: Game of Life on the dock, plus Portfolio, Epiphany, Keyrate, Quotes as real apps. The portfolio site root is now the OS itself booting in portfolio mode, idle demo opens apps live, old homepage moved to classic.html.

Monday fixed the Dock launcher silently failing: the Mac app moved to its own repo but still looked four folders up. Added logging, merged window dragging and snapping. One-point-zero: polish and QA. Frame-by-frame headless testing caught bugs forty tests missed: Files crash, Terminal crush, Calendar's last week. Security audit found password in memory post-hash. Disk hit zero mid-run. Recovered 64 GB by clearing Xcode DerivedData, caches, watchOS simulator (9 GB), Claude VM bundles (10 GB), erasing simulators, and symlinking Steam to the 2TB LaCie drive; now keeps 20+ GB headroom and big jobs on LaCie. Usage ran out before 1.0 shipped.

Fixed the landing demo spitting HTML on weather: wttr.in serves full pages now, demo only checked for degree signs. She asks JSON, capped at 1200 chars per reply. Added caption to her T logo; wrote SECURITY.md. Web_demo.py tests weather, bad places, every chip, idle-reel lines, fails on runaway/raw-page. Caught: complex logo chip never routed (no adjectives before logo). Fixed both. Evals all green: 77/77 actions, 77/77 web, zero failures.
